class solution:
def nextgrestestletter(slef,letters:list[str],target:str)->str:
  left,right=0,len(letters)
  while(left<right):
    mid=(left+right)//2
    if(letters[mid]>target):
      right=mid
    else:
      left=mid+1
  return letters[left%len(Letters)]
