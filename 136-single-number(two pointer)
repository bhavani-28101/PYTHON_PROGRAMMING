class Solution(object):
    def singleNumber(self, nums):
        nums=sorted(nums)
        i,j=0,1
        while((i<len(nums)-1) and (j<len(nums)-1)):
            if(nums[i]!=nums[j]):
                return nums[i]
                break
            i=j+1
            j=i+1
        return nums[i]
