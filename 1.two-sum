class Solution(object):
    def twoSum(self, nums, target):
        seen={}
        diff=0
        for i in range(len(nums)):
            diff=target-nums[i]
            if diff in seen:
                return [seen[diff],i]
            seen[nums[i]]=i
