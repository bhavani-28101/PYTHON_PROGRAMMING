class Solution(object):
    def findDisappearedNumbers(self, nums):
        res=[]
        num=set(nums)
        for r in range(1,len(nums)+1):
            if r not in num:
                res.append(r)
        return res
