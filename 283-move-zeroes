class Solution(object):
    def moveZeroes(self, nums):
        k=0
        count=0
        for r in range(len(nums)):
            if(nums[r]!=0):
                nums[k]=nums[r]
                k+=1  
        for i in range(k,len(nums)):
            nums[i]=0
