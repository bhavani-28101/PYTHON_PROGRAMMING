class Solution(object):
    def isGood(self, nums):
        nums.sort()
        n = nums[-1]   # max element
        
        # expected pattern
        expected = list(range(1, n)) + [n, n]
        
        return nums == expected
